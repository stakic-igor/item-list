import React from 'react';

const ListItem = ({
    name, id
}) => {
    return (
        <div>
            <div></div>
            <div>{id} - {name}</div>
        </div>
    )
}

export default ListItem;
